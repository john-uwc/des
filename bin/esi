#!/usr/bin/env bash

# author: steven, date:2017.3.31
# es cli

source "$(cd $(dirname ${BASH_SOURCE[0]}) && pwd -P)/../frw/.ins"

if [ "--sc" == "$1" ]; then
_include "bin/frw.cks/util"; _include "bin/frw.cks/sys"; exit
fi

# start and ready go
_include "core/startup" && fs cd $(pwd)