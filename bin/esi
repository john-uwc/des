#!/usr/bin/env bash

# author: steven, date:2017.3.31
# es cli

# root location calculate
declare -r _root_=$(dirname $(cd $(dirname "${BASH_SOURCE[0]}") && pwd))

source "$_root_/frw/install"

if [ "--sck" == "$1" ]; then
_include "bin/icks/util.frw"; exit $_err_s_
fi

# start and ready go
_include "core/startup"; fs cd $(pwd)