#!/usr/bin/env bash

# author: steven, date:2017.3.31
# es cli

source "$(cd $(dirname ${BASH_SOURCE[0]}) && pwd)/../frw/.ins"

if [ "--sck" == "$1" ]; then
_include "bin/icks/util.frw"
_include "bin/icks/sys.frw"
exit $_err_s_
fi

# start and ready go
_include "core/startup" && fs cd $(pwd)